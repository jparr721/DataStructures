class Node:
    def __init_(self, data, left, right):
        self.data = data
        self.left = left
        self.right = right

def btree(ls):
    return make_btree(ls, 0, len(ls) - 1)


def make_btree(tree, l, r):
    pass
